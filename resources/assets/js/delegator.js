// var delegator = {} || null;

// delegator.delegate = function (element, event, url, method, data, headers) {
// 	request : app.requester.load(),
// 	delegator : function () {
// 		$(document).delegate(element, event, function(event) {
// 			event.preventDefault();

// 			request[method](url, null, data).then(
// 				function (success) {
// 					delegationCallback(success);
// 				},
// 				function (fail) {
// 					delegationCallback(fail);
// 				}
// 			);
// 		});	
// 	}

// 	delegationCallback : function (callback) {
// 		return callback;
// 	}
// }